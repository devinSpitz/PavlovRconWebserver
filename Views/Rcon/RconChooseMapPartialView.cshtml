@model List<RconMapsViewModel>

@section Scripts{
    <script>

    </script>
    
}

<!-- Modal -->
<div class="modal fade" id="add-contact" tabindex="-1" role="dialog" aria-labelledby="addContactLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="addContactLabel">Choose item</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">×</span>
                </button>
            </div>
            <div class="modal-body">
                <div class='search'>
                <input  class="col-sm-6" id='searchAuthor' type="text" placeholder='Search for authors' name="search"> 
                <input class="col-sm-6" id='searchName' type="text" placeholder='Search for map name' name="search"> 
                </div>
                @foreach (var map in @Model)
                {
                    <div class="col-sm-6">
                        <div class="panel panel-default mapPanel" data-Author="@map.Author" data-Name="@map.Name">
                            <div class="panel-heading">
                                @map.Name - @map.Author
                            </div>
                            <div class="panel-body">
                                <button data-dismiss="modal" onclick="setMap('@map.Id')"><img src="@map.ImageUrl"></button>
                            </div>
                        </div>
                    </div>
                }

                <button type="button" class="btn btn-danger" data-dismiss="modal">Cancel</button>
            </div>
        </div>
    </div>
</div>