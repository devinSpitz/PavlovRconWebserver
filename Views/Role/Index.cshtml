@model System.Collections.Generic.IEnumerable<LiteDB.Identity.Models.LiteDbRole>
<h1 class="">All Roles</h1>
<div class="bg-info">
    hierarchy: 
    <ul>
        <li>Admin</li>
        <li>Mod</li>
        <li>Captain</li>
        <li>User</li>
    </ul>
</div>
<table class="table table-sm table-bordered table-bordered">
    <tr><th>ID</th><th>Name</th><th>Users</th></tr>
    @foreach (var role in Model)
    {
        <tr>
            <td>@role.Id</td>
            <td>@role.Name</td>
            @*<td i-role="@role.Id"></td>*@
            <td><a class="btn btn-sm btn-primary" asp-action="Update" asp-route-id="@role.Id">Update</a></td>
            @*<td>
                <form asp-action="Delete" asp-route-id="@role.Id" method="post">
                    <button type="submit" class="btn btn-sm btn-danger">
                        Delete
                    </button>
                </form>
            </td>*@
        </tr>
    }
</table>